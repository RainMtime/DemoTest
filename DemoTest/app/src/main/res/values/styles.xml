<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <declare-styleable name="PullDownAnimView">
        <attr name="paint_width" format="dimension"/>
        <attr name="paint_color" format="color"/>
    </declare-styleable>

    <style name="AppTheme.Black">
        <item name="skin_btn1">@color/colorAccent</item>
        <item name="skin_drawable">@drawable/ic_menu_camera</item>
    </style>

    <style name="AppTheme.Light">
        <item name="skin_btn1">@color/colorPrimary</item>
        <item name="skin_drawable">@drawable/ic_menu_gallery</item>
    </style>

    <declare-styleable name="SwipeFlingAdapterView">
        <attr name="y_offset_step" format="dimension" />
        <attr name="rotation_degrees" format="float" />
        <attr name="min_adapter_stack" format="integer" />
        <attr name="max_visible" format="integer" />
    </declare-styleable>


</resources>
